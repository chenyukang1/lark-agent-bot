[project]
name = "lark-agent-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "lark-oapi>=1.4.8",
    "python-dotenv>=1.0.0",
]
