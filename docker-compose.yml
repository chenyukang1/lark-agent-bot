services:
  lark-agent-bot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./feishu_mapping.json:/app/feishu_mapping.json
      - ./codebase_configs.json:/app/codebase_configs.json